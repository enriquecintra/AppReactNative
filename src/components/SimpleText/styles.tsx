import { StyleSheet } from 'react-native';
import { Colors } from '../../styles';

export const styles = StyleSheet.create({
    textSimples:{
        color: Colors.PRETO,
        fontSize: 20
    },
    textoBotaoPurble:{
        color: Colors.BRANCO,
        fontSize: 20
    }
});